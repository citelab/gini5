# --- Python ---
__pycache__/
*.pyc
*.egg-info/
# build artifacts — never commit (keeps the tree clean for setuptools-scm)
dist/
.pytest_cache/
pytest-cache-files-*/
.venv/
.mypy_cache/
.ruff_cache/

# --- C router build ---
*.o
# `*.zigobj.o` alone did not match the real artifacts, which are `*.zigobj.o.tmpXXXXXXX` — which is
# how six of them stayed tracked through the Zig removal and were still in the tree afterwards.
*.zigobj.o*
config.log
backend/build/
# legacy: Zig removed; kept so any stray cache never re-enters git
backend/.zig-cache/
bin/
build/
lib/
share/
root_fs_beta2
.sconf_temp/
.sconsign.dblite

# --- editors / OS ---
.idea/
.settings
.cproject*
.project*
.pydevproject*
.ctags*
.tags*
tags
*.swp
*~
.DS_Store

# --- artifacts / scratch ---
*.deb
.__unlink_test
# offscreen render previews from scripts/gen_cue_shots.py and ad-hoc checks
cue_*.png
frontend-ng/src/gini/data/kb_index.json

# setuptools-scm generated version file
frontend-ng/src/gini/_version.py
core/src/gini/_core_version.py
teaching-center/src/gini_teaching_center/_version.py

token.txt
*.token

token.txt
*.token

# Teaching Center runtime data: the course SQLite database (accounts, activities, and student
# submissions), created by teaching-center/run.sh. Never source — and `commit.sh` does `git add -A`,
# so without this line a test drive quietly commits a database with real people in it.
tc-data/
teaching-center/example/data/
*.db-wal
*.db-shm
